import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carcheck.brunoravanello',
  appName: 'car-check',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
